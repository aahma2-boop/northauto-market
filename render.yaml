services:
  - type: web
    name: northauto-market
    env: node
    plan: free
    nodeVersion: 24
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
